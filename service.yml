apiVersion: v1
kind: Service
metadata:
  name: ms-product-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: ms-product
  ports:
  - protocol: TCP
    port: 30002
    targetPort: 8080
    name: default
